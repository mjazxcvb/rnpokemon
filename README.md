# RNPokemon
react-native and typescript

sample pokemon app


## How To Use

#### Install dependencies:

``npm install``

#### Run in iOS:

``cd ios && pod install ``

``cd <projectDir>`` 

``npx react-native run-ios``

_note: only tested in ios_
